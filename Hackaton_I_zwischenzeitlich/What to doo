zusatzziel:
auswertung aller runden

Args:
    Time(float): zeit die user für eine eingabe gebraucht hat
    distance(float): entfernung zur landmarke
    pints(distance * time): finale punkte
    runde(die aktuelle zahl der runden)

alle drei werte in jeweils ein dict packen key = round, val = wert
fpr alle drei dicts folgende werte ausrechnen:
durchschnitt
beste(schnellste) runde
schlechteste(schnellste) runde
anzahl runden (länge der listen)
median


=======================

zusatzziel:
Args:
    url liste: liste von urls, aus denen ein landmarker ausgewählt wird
aus liste von urls wird eine url ausgewählt.
die url, die ausgewählt wird, aus der liste löschen


bsp. liste:
runde, entferung, zeit, punkte
liste 2 = [
[1, 34, 22, 34x22, "url1"]
[2, 45, 52, 45x52, "url2"]
[3, 55, 11, 55x11, "url3"]
]